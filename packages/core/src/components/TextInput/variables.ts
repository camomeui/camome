import { ComponentCssVariables } from "../../types";

const variables = {
  leftDecorWidth: "0.75rem",
  rightDecorWidth: "0.75rem",
  margin: null,
} as const satisfies ComponentCssVariables;

export default variables;
